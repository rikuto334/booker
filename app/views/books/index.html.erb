<h1>Books</h1>

<% @books.each do |book| %>
  <p>title</p>
  <span>
    <%= book.title %>
  </span>
  <p>body</p>
   <span>
     <%= book.body %>
   </span>
   <span>
     <p><%= link_to "show", book_path(book.id) %></p>
   </span>

<%end%>